# Upper bound is deliberate. The pages call st.altair_chart(..., use_container_width=True),
# which Streamlit has deprecated with a stated removal date that has already passed. It
# still works in 1.62, but a future release will drop it. Capping here means
# `pip install -r requirements.txt` installs the version this project was tested against,
# so the app cannot break between now and the demo.
streamlit>=1.30,<1.63
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
altair>=5.5          # alt.theme.register (used in ui/charts.py) needs 5.5+
matplotlib>=3.7
pytest>=7.0
