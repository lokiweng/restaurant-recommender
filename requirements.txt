# Upper bound is deliberate: it pins the Streamlit version this project was
# tested against, so `pip install -r requirements.txt` reproduces the
# environment the reported results were produced in and the app cannot change
# behaviour between now and the demonstration.
streamlit>=1.30,<1.63
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
altair>=5.5          # alt.theme.register (used in ui/charts.py) needs 5.5+
matplotlib>=3.7
pytest>=7.0
