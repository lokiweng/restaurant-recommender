"""
ui -- presentation only.

Design tokens, the stylesheet, and the reusable visual components. Nothing in
this package computes a recommendation; nothing in core/ knows this package
exists. Pages compose the two.
"""
